# Continuing repository audit — 12 September 2026

This pass starts at `99e197c` and continues the earlier audits. The audit remains
open: this is a record of reproduced issues and measured behavior, not a claim
that every line or browser is now defect-free. Changes are local commits; no
release was pushed or deployed.

## Fixed in this pass

| Commit | Reproduction | Change and browser evidence |
| --- | --- | --- |
| `6326698` | At 375px wide and 200% root text, Resign has 116px for a 125px label. | The mobile Engine Lab button rule also matched Play controls and overrode their content minimum. Restored that minimum for action rows. Removed the browser test's explicit exemption; desktop/mobile board, dialogs and skip links pass. |
| `6c34fc3` | Reload a cached production build on a server with HTTP caching disabled: four unchanged build assets still reach the network. | Serve content-hashed Vite JS/CSS from Cache Storage. Warm reload sends zero requests for those four assets. New hashes, documents and version-named engine files still use the network. A cached JS chunk remains readable after its server copy is removed. |
| `4cc9431` | Open Commands, Tab to Flip board, press F: the board flips behind the dialog. N can also open another modal. | Commands now participates in background inertness and shortcut suspension. Its own Ctrl/Cmd+K toggle, search editing, Enter selection and focus restoration continue to work. Checked at 1280px and 375px. |
| `e7f4dbb` | Prime the offline cache, make the host return HTTP 503, reload: the server error replaces the app. | A cached response wins over a server error. A real Chromium reload still reaches a board with cross-origin isolation; uncached requests retain 503 and a 403 is not concealed. |
| `551e887` | Abort the New Game JS download. The error notice says the board is unaffected, but its parent and toolbar remain inert. | The error boundary closes the failed overlay and announces the failure. It resets when the requested dialog changes. Two failed attempts still allow e2–e4 and opening PGN Export, at desktop and phone widths. |

Each implementation commit was followed by browser QA. Network-policy tests use
a real service worker on a local host without COOP/COEP headers. UI regressions
use deterministic UCI fixtures; the engine benchmark below uses real WASM.

## Performance measurements

### Returning visits

The immutable-asset change removes four network requests from the tested warm
startup. This is a request count, **not** a claimed percentage improvement in
page-load time. The document, engine assets and any previously unseen chunks may
still need the network. The cache check deliberately disables the HTTP cache so
that the browser's own freshness heuristics cannot account for the improvement.

The policy is limited to hashed JS/CSS beneath this service worker's `assets/`
path. A Range request still reaches the network and returns 206. Offline loads
retain the isolation headers needed for multithreaded Stockfish.

### Review throughput: the existing pool

Re-ran `scripts/benchmark-review-browser.cjs` against the actual Stockfish 18 Lite
single-thread WASM build. Opera Game, 33 targets, depth 18, 64MB total Hash,
Chromium reporting 18 logical cores; fresh workers in A–B–B–A order.

| Workers | Run 1 | Run 2 | Mean |
| --- | ---: | ---: | ---: |
| 1 | 16.707s | 16.248s | 16.478s |
| 4 | 8.226s | 8.107s | 8.167s |

The existing pool is **2.02× faster** on this fixture and machine, about 50% less
wall time. Every run scored all 33 targets at the requested depth. This does not
establish identical move grades, universal speedup, or low-memory-device safety.
No pool algorithm changed in this pass. The older audit's 5× result used another
game, depth and environment and must not be presented as this run's result.

[Raw benchmark output](qa/2026-09-12/review-benchmark.json).

## Stockfish and reference-repository research

- [Stockfish 19](https://stockfishchess.org/blog/2026/stockfish-19/) was released
  on September 5. It adds native WebAssembly targets and stricter position/UCI
  validation; invalid input can terminate the engine. A migration needs explicit
  handling and tests for this failure mode.
- The app's provider,
  [nmrugg/stockfish.js](https://github.com/nmrugg/stockfish.js), still documents
  Stockfish 18. On this date `npm view stockfish version` returned `18.0.8`; the
  app's full CDN URLs pin `18.0.7`. The local lite build and the full browser build
  are different strength/download choices. The full build exceeds 100MB; making
  it every beginner's default would impose a substantial download.
- [lichess-org/stockfish-web](https://github.com/lichess-org/stockfish-web)
  includes SF19 and SF19 small-network builds, but explicitly describes its
  integration as more involved. It is an adapter/build migration, not a safe
  substitution of a version number in the current worker URLs.
- [Lichess's evaluation controller](https://github.com/lichess-org/lila/blob/master/ui/lib/src/ceval/ctrl.ts)
  throttles updates, reuses completed search effort, and unloads a worker when
  another tab starts analysis. This app already batches engine updates and
  caches finite navigation searches. Cross-tab resource ownership is a useful
  next comparison, with care to preserve intentionally running game reviews.
- [Nibbler](https://github.com/rooklift/nibbler) exposes constrained root searches,
  PV previews, arbitrary variations and Chess960. This app already has the first
  three. Chess960 remains a rules/PGN/board/engine project, not a missing switch.
- [Vite's production guidance](https://vite.dev/guide/build.html) describes old
  dynamic-import filenames disappearing after deployments. The cache and dialog
  fixes above address the cached and uncached cases separately.

Decision: keep the working SF18 browser integration for now. Evaluate the 18.0.8
provider patch separately; prototype SF19 behind a distinct engine adapter and
compare download, boot time, search throughput, memory and position validity
before changing the default.

## Highest-value next work

| Priority | Work | Why / acceptance evidence |
| --- | --- | --- |
| 1 | Record engine/profile provenance in evaluations and completed reviews. | `EvalSnapshot` carries depth, nodes and purpose but no engine identity. The shared FEN map prefers depth across incoming results. Switching from lite to full should have an explicit policy, a visible source, and a regression showing which result the review uses. This is a confirmed model gap; the misleading UI scenario still needs a controlled reproduction. |
| 1 | Continue desktop/mobile input and modal auditing. | The defects above crossed CSS specificity, focus, history and failure-state boundaries. Add Firefox/WebKit coverage and test a real iOS keyboard, safe areas and rotation; Chromium viewport emulation is not that evidence. |
| 2 | Profile and split the busiest App subscriptions. | `App.tsx` is over 8,600 lines. Engine snapshots still enter the shared component at roughly 100ms intervals. Measure scripting/layout/interaction latency on a long annotated game and a throttled device; extract board, review and settings boundaries where the trace shows work. File size alone is not a speed measurement. |
| 2 | Make engine resource ownership visible across tabs. | Compare the Lichess approach and measure total worker heaps/CPU. Preserve finite background reviews; avoid two unattended infinite searches saturating the same machine. |
| 2 | Bound retained deployment assets. | The runtime cache keeps old hashed assets under one cache version. Add an eviction policy only with tests protecting open tabs, offline startup and saved-game storage. The current change does not prune it. |
| 3 | Prototype SF19 with comparison fixtures. | Keep engine strength distinct from nodes/second. Include underpromotions, repetition history, legal unusual material, malformed FEN/UCI and fallback boots. |

## Features worth building next

1. **Repertoire study with a due queue.** Build on the existing saved variation
   trees and line drills: chapter names, side to train, remembered attempts,
   due dates, and a short daily session. Keep imports/exports lossless. Useful
   both to a beginner learning one opening and to a tournament player reviewing
   a repertoire.
2. **Calibrated easier opponents and staged lessons.** The current lowest
   Stockfish setting is advertised around 1320 Elo, already strong for a new
   player. Measure actual playing behavior rather than assigning a smaller Elo
   label; add exercises for piece safety, mating patterns and basic endings with
   explicit success criteria.
3. **Comparable saved analysis runs.** Preserve engine version/profile, limits,
   depth, timestamp and the line reviewed. Let advanced users compare two runs
   without silently mixing their evaluations or changing a finished report.
4. **Chess960 after a rules-layer design.** Add only when castling, imports,
   exports, board input and UCI position encoding can be tested together. This
   expands serious-player coverage but is larger than the fixes above.

## Validation and coverage

- `npm run verify`: typecheck, lint, tests and production build. 1,559 tests
  pass and one is skipped. Lint reports three existing dependency warnings
  involving the review flush callbacks, and zero errors.
- `npm run audit`: zero reported vulnerabilities.
- Full post-change Chromium browser gate: passed at `551e887`, including
  desktop/phone/landscape, dark/light themes, keyboard and touch interactions,
  200% text, game/library/settings round trips, all new regressions and the
  actual offline service worker. [Browser log](qa/2026-09-12/browser-qa.txt).
- Real Stockfish review benchmark: four completed runs, described above.
- Real-engine smoke checks: successful UCI searches and e2–e4 at 1440×900,
  375×812 and 844×390; 64 squares, no page errors or horizontal document
  overflow. Phone viewports used Chromium with iPhone device emulation and
  selected the single-thread engine; desktop selected the multithread engine.
  This is not a Safari compatibility test.
- Read in this pass: application modal/shortcut wiring, mobile action CSS,
  service worker, build and CI configuration, engine profiles/bootstrap and
  relevant hook portions, review-pool planning, evaluation replacement, FEN
  validation, opening/cloud-cache hooks, dialog boundary and browser harness.
  Earlier audit documents were used as context; their claims were not treated
  as fresh verification.

The first broad browser run overlapped a later build and encountered a missing
lazy chunk. The isolated target-size check then passed. Builds and browser runs
must be sequenced against one artifact; the final suite runs without changes to
`dist/`. The incident also led to the independently reproduced dialog failure
fix above.

### Visual evidence

| Viewport | Play | Analysis |
| --- | --- | --- |
| Desktop | [Screenshot](qa/2026-09-12/desktop-play.png) | [Screenshot](qa/2026-09-12/desktop-analysis.png) |
| Phone | [Screenshot](qa/2026-09-12/mobile-play.png) | [Screenshot](qa/2026-09-12/mobile-analysis.png) |
| Landscape phone | [Screenshot](qa/2026-09-12/landscape-play.png) | [Screenshot](qa/2026-09-12/landscape-analysis.png) |

[Phone at 200% text](qa/2026-09-12/mobile-large-text.png) ·
[Real-engine smoke results](qa/2026-09-12/real-engine-smoke.json).
